services:

  # ─────────────────────────────────────────────
  # Production 專用覆蓋設定
  # ─────────────────────────────────────────────
  # nuxt 服務已定義於 docker-compose.yml（共用）
  # 未來可在此加入 production Dockerfile 覆蓋：
  #
  # nuxt:
  #   build:
  #     context: ..
  #     dockerfile: docker/frontend/Dockerfile.prod
  #   volumes: []  # production 不掛載源碼
  #
  # php-fpm:
  #   environment:
  #     - CI_ENVIRONMENT=production
